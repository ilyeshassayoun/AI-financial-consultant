# Install the runtime application stack before test-only tools. This keeps a
# fresh developer virtual environment able to import FastAPI application tests.
-r requirements.txt

# Development dependencies
pytest==8.3.3
pytest-asyncio==0.23.7
